# Growth signal inbox

> **Append one line the moment you notice something. Don't analyze — just capture. (Analysis comes later — /fable-team:grow does it.)**
>
> Format: `- [ ] date | mission | type | what happened (one line)`
>
> Types:
> - **Correction** — feedback or a correction from the user (highest-priority signal; even a single one is a candidate for distillation)
> - **Rework** — something had to be redone (include the cause)
> - **Failure** — an escalation occurred (e.g., builder failed twice)
> - **Surprise** — an assumption was wrong, or reality differed from expectations
> - **Friction** — repeating the same setup every time, information hard to find, heavyweight procedures
> - **Success** — a pattern that worked well (worth reproducing)
>
> When processed, mark it `[x]` and leave an entry in `changelog.md` (**Discarding is a valid outcome.**).

## Signals

- [x] 2026-07-14 | task: git-branch-tree | Rework | builder が「初期値空 = 全フォルダ閉じ」と自己申告したが、実装は `collapsed` Set + `isOpen = !collapsed.has()` で論理が反転しており、実際は全部開いていた。typecheck は通るため型検査では捕まらず、挙動検証で初めて発覚
- [x] 2026-07-14 | task: git-branch-tree | Friction | claude-deck 自身を開発対象にすると、ユーザー稼働中のインスタンスと dev サーバのポート (8110/3711) が衝突し、さらに新クライアントが稼働中 PTY セッションに再接続してブラウザタブがフリーズして検証できなかった
- [x] 2026-07-14 | task: git-branch-tree | Success | 本体アプリが重くて開けないとき、対象コンポーネントだけを単独マウントする使い捨ての Vite ページ (client/*.html + entry.tsx) を立て、DOM を evaluate_script で直接アサートする検証が速くて確実だった
- [x] 2026-07-20 | task: terminal-clipboard | Surprise | ConPTY は子プロセスの DECSET を選別転送する: ?2004 (bracketed paste) と OSC 52 はホストへ通すが、マウストラッキング (?1002/?1006) は素の probe では飲み込んだ。「エスケープシーケンスは素通し」という前提は Windows では成立しない
- [x] 2026-07-20 | task: terminal-clipboard | Success | 別ポート (3799) にテストサーバーを立て、ページ内から side-WebSocket で PTY 入力を注入 + navigator.clipboard と WebSocket.send をモンキーパッチして観測する方式で、キーボードシミュレーション無しにクリップボード/ペーストの E2E 検証ができた
- [x] 2026-07-20 | task: non-git-dirs | Correction | 「非 git ディレクトリー対応」の初回プランで git repo の下位ディレクトリーを「一貫して非 git 扱い」に倒したが、ユーザーは下位ディレクトリーで作業することがあり Git 機能有効を要望。ディレクトリースコープの機能設計では root/subdir/none の 3 状態を最初から検討すべき
- [x] 2026-07-20 | task: non-git-dirs | Success | サーバー検証を PORT=3811 + USERPROFILE をスクラッチに差し替えて起動することで、稼働中インスタンス・実設定 (~/.claude-deck3/config.json) と完全隔離した API 検証ができた(config.ts が os.homedir() 起点なため)
- [x] 2026-07-20 | task: non-git-dirs | Friction | USERPROFILE を差し替えると Volta シムの node/npx が不安定になる(LocalAppData を見失う)。pinned node.exe で tsx の CLI (node_modules/tsx/dist/cli.mjs) を直接叩けば回避できる
- [x] 2026-07-20 22:17 | surprise: tsconfig の extends は exclude をマージしない(build 側 tsconfig にも test 除外が必要。builder C 実測)
- [x] 2026-07-20 22:17 | success: 同一ファイルの並列編集は brief で編集領域を明示的に分割すれば worktree 隔離なしで成立(server/git.ts、builder 2 体)
- [x] 2026-07-20 22:41 | success: レビュー指摘の小修正は元 builder へ SendMessage 継続が速い(コールドスタート回避、attempt 2 は約 2 分で完了)
- [x] 2026-07-21 00:57 | success: ハイリスク純関数の brief に「実物を観察してから fixture を作れ」「apply --check まで通せ」を入れたことで builder が実バグを実装中に自力検出した(git-client-parity 2.1)
- [x] 2026-07-21 | task: editor-state-persistence | Rework | 新モジュール(editorState)の brief で既存モジュールから定数 import させたら、後続タスクで逆方向 import が必要になり循環解消の手戻り。共有定数は依存の最下流(新モジュール側)に最初から置かせるべき
- [x] 2026-07-21 | task: editor-state-persistence | Rework | localStorage sanitize にサイズ上限を入れたら reviewer が「書き込み側が無制限」の非対称を検出(未保存編集の無警告消失窓)。読み捨てる上限には書き込み側の対称ガード+ユーザー通知をセットで設計する
- [x] 2026-07-21 | task: editor-state-persistence | Success | Monaco は隠し textarea が aria-hidden で CDP の実キー入力がブロックされる。React Fiber から editor インスタンスを取得し executeEdits() を呼べば onChange→dirty→debounce の実経路ごと検証できる(verifier 実証)
- [x] 2026-07-21 | task: editor-state-persistence | Surprise | 選択中 worktree は 4 秒ポーリングがファイルハンドルを掴むためか Windows では git worktree remove が 500 で失敗する(force でも)。別 worktree へ切替後は成功。本機能と無関係の既存問題(verifier 発見、未修正)
- [x] 2026-07-21 | mission: git-client-parity | Success | セッション中断で死んだ builder を SendMessage 再開し「まず自分の部分差分を git status/diff で検証→続行」パターンが機能。中断報告は失われてもトランスクリプトと作業ツリーから完全復帰できた
- [x] 2026-07-21 | mission: git-client-parity | Success | フェーズゲート reviewer が並行編集によるハンク誤破棄を実 git で再現して検出(hunkCount のみの楽観ロックの盲点)。不可逆操作の楽観ロックは「数」でなく「対象の同一性」まで検証する — 設計原則として距離できる
- [x] 2026-07-21 | 隔離 USERPROFILE で Volta シムが死ぬ(builder は node.exe 実体 + tsx/dist/cli.mjs 直叩きで回避)— pj-isolated-verify への追記候補 [friction]
- [x] 2026-07-21 | E2E ゲートが実害バグを掘り当てた(PromptDialog 連続プロンプトの state 残留 — Promise ベース連続モーダルは request 毎の key 再マウントが安全パターン)。unit green でも UI 状態バグは E2E でしか出ない [success-pattern]
- [x] 2026-07-21 | E2E ゲート通過後に敵対的レビューが実害インジェクションを発見(rename に -f)— 自由入力を git 引数に渡す新規コードは「-- セパレーター」をチェックリスト化する価値あり [failure→pattern]
- [x] 2026-07-21 19:23 | friction | 隔離検証のスクラッチを深いパスに掘ると Windows で git rebase が Filename too long — pj-isolated-verify に「フィクスチャは短パス(例 C:\vt5)推奨」を追記したい(5.V で実測・切り分け済み)
- [x] 2026-07-21 19:44 | success | 同型 UI を別コンポーネントに実装すると disabled 条件が非対称になりやすい(HistoryTab のコミットメニューが busy を見落とし、GitTab の rebase 項目だけ busy ガード)。新規 UI 配線は既存の対称物の disabled 条件を突き合わせるチェックを — 5.R が指摘
- [x] 2026-07-22 01:37 【驚き】`String(req.body.x ?? '')` 検証は配列 body を "a,b" に化かして素通し — typeof チェックを先に置く必要(6.1 builder が敵対的テストで実測・修正)。既存ルートも同型の可能性 → pj-git-route 防壁節への追記候補
- [x] 2026-07-22 01:56 【摩擦】既存 stash-apply/drop は git.ts 内検証+asyncHandler で不正 ref が 500(400 でない)— pj-git-route「検証はルート側」原則と非対称(6.2 builder 実測)。6.R 判断材料+プレイブック追記候補
- [x] 2026-07-22 02:17 【成功パターン】`--follow` の per-commit 旧パスは `--name-status` 併用で取得し diff に origPath を必ず渡す — リネーム前コミットが「新規ファイル」に化ける誤表示を防ぐ(6.3 builder 隔離実測)。pj-git-route 追記候補
- [x] 2026-07-22 02:32 【成功パターン】git log の自由入力検索は `--fixed-strings -i` + `--author=`/`--grep=` の `=` 埋め込み単一トークン — regex メタ文字 500 と `--upload-pack=` 系注入の両方を実測で封じる(6.4 builder)。pj-git-route 追記候補
- [x] 2026-07-22 03:03 【成功パターン】ブラウザー MCP 無し環境でも Node 組込み WebSocket の CDP 直叩きで実機 E2E が成立(Runtime.evaluate + Page.handleJavaScriptDialog で native prompt 応答、consoleAPICalled 購読で新規エラー監視)— 6.V verifier 自作 cdp.mjs。pj-isolated-verify 追記候補

- [x] 2026-07-22 15:58 【成功パターン】部分パッチの未選択行の扱いは **git apply の適用方向で逆転**する(forward=未選択 `-` を context 化、reverse=未選択 `+` を context 化)。discard は diff 取得元が worktree でも reverse 側という取り違えやすい組み合わせ — pj-git-route 追記候補(2.4a builder が実 git 6 パターンで実測)
- [x] 2026-07-22 15:58 【驚き】`git apply` は部分パッチの `@@` 開始行番号のずれを forward/reverse とも許容する(fuzzy match)。ただし内部整合の取れたパッチを作る方が安全側 — 「git が通した = 正しい」で検証を終えない根拠になる(2.4a 実測)

- [x] 2026-07-22 16:40 【失敗→パターン】vitest 86 件 green が実データ破壊バグを 1 件も捕まえなかった — 実装者が自分の出力を期待値に固定したため。**パッチ生成系の純関数テストは「実装が返した値」でなく「実 git に apply した後の post-image が意図どおりか」を期待値の基準にする**よう brief で強制する(2.4V が実機で検出)
- [x] 2026-07-22 16:40 【驚き】部分パッチは pre-image さえ整合していれば `git apply` が成功するため、**post-image の行順序が壊れていても検出されない**。「apply が通った」を合格判定に使ってはいけない(2.4V 実測: line1,line3,line2-CHANGED,line4)
- [x] 2026-07-22 16:40 【成功パターン】Conductor がコード実読で予測した論点(ヘッダーのみの楽観ロックは行内容変化を検出できない)を verifier の検証項目に明示的に載せたところ、実データ混入として再現された。**受入時のスポットチェックで見つけた「疑い」は検証項目に降ろす**と実害の有無まで確定できる

- [x] 2026-07-22 20:10 【失敗→パターン】`\ No newline at end of file` marker を「内容行に紐付けて動かす」実装は、その行が context 化されて**後続に出力行が残る**と marker の意味(=両側で最終行・改行なし)と矛盾し、git が後続行を融合させる。**末尾改行なしファイルへの行追記**という日常操作で発火し、`git apply` は 200 を返す。部分パッチ生成では marker の**位置の妥当性**を不変条件として検査する必要がある(2.4R が実証)
- [x] 2026-07-22 20:10 【驚き】`hunksUtf8` 上での内容比較は**非可逆デコード**のため非 UTF-8 ファイルで別バイト列を同一と判定する(SJIS「あ」`82a0` と「い」`82a2` が utf8 デコードで一致)。**楽観ロックの照合は生バイトのハッシュで行う**べき — 添字の対応づけは utf8/latin1 で一致するが、内容比較は一致しない(2.4R 実証)
- [x] 2026-07-22 20:10 【成功パターン】reviewer に「壊せなかったこと」も報告させると価値が高い(前提の反例探索 11,200 サンプルで 0 件、バイト不変性 91 ケースで違反 0 等)。「探したが見つからなかった」と「探していない」の区別が、後続フェーズの再調査コストを消す
- [x] 2026-07-22 20:10 【驚き】`server/index.ts` はモジュールスコープで `express()`・`new PtyManager(PORT)`・`app.listen` を実行するため、supertest 導入は**実サーバーと PTY 起動**を伴う = ルートテスト基盤の追加は「ルート定義の別モジュール抽出」とセット。代替は**検証ロジックの純関数抽出**(新規依存ゼロ)— pj-git-route 追記候補(2.4R 実測)

- [x] 2026-07-22 22:10 【驚き】実 git は **context 行にも `\ No newline at end of file` marker を付ける**(最終行が未変更で末尾改行が無い場合)。「marker は必ず `-`/`+` に付く」という前提でロジックとテスト戦略を組むと、結論がたまたま正しくても将来の変更で崩れる(2.4R が実証、builder のコメントが誤り)
- [x] 2026-07-22 22:10 【失敗→パターン】builder が「実 git で到達可能な入力なら必ず解消する」と**証明を主張**したが、reviewer がランダム 97 ケースで反例 3 件を発見。**証明の主張はレビューで反例探索の対象にする**(証明が正しければコストはゼロ、誤っていれば機能欠落が見つかる)
- [x] 2026-07-22 22:10 【成功パターン】不可逆操作の許容判定は「頻度」ではなく「**確認ダイアログの申告が実際と一致するか**」で切る。2.4R は発生率 0.1% 未満と実測したうえで、なお (a) 許容を退けた — pj-git-route §4「影響件数は正確に」の再確認。可逆な操作(stage/unstage)とは許容度が違う

- [x] 2026-07-23 08:30 【成功パターン】パーサーの検証は**差分オラクル**が強い — 同じ情報をより冗長な形式で出す別コマンド(`git blame --line-porcelain` は行ごとに全メタデータを繰り返すため dedup 推論が不要)を独立実装でパースし、本実装と全フィールド突合する。ランダム履歴フューズ 180 実行 / 3,682 行で全一致を示せた(6.5R)
- [x] 2026-07-23 08:30 【成功パターン】**変異テストでテストの質を機械的に測る** — 実装者が実際に犯した誤解を変異として注入し、テスト群が殺せるかを見る。6.5R は 14 変異中 9 を KILLED、生存 4 件がそのままテストの穴の指摘になった。「テストが実装をなぞっていないか」を主観でなく数字で言える(2.4 でその事故が起きた後の対策として有効)
- [x] 2026-07-23 08:30 【成功パターン】読み取り専用性の証明は**対照実験**で — 攻撃前後のスナップショット比較だけでなく、**stale index を作って「書き込む操作なら検出できる感度がある」ことを対照の `git status` で示す**。「差分が無かった」を「検出力があるうえで差分が無かった」に格上げできる(6.5R)
- [x] 2026-07-23 08:30 【驚き】`git blame` は生バイトを `0x0A` で分割するため、**UTF-16LE(改行が `0A 00`)では 2 行目以降の先頭に孤児の `00` が残り 1 バイトずつずれる**。行単位デコードでは原理的に直せない。UTF-16BE は `00 0A` で偶然無傷 — Windows 主体の環境では LE が多数派なので実害が出る(6.5R 実証)
- [x] 2026-07-23 08:30 【失敗→パターン】正規表現に**不一致だった行を黙って捨てる**(`if (!m) continue`)実装は、フォーマット不一致を必ず「空/行欠落」に変換し、**正当な空の結果とバイト単位で区別できない誤答**を生む。捨てた件数を数えて 1 件でもあれば異常系に倒すべき(6.5R が SHA-256 リポジトリーで実証)
- [x] 2026-07-23 08:30 【摩擦】変異テストのサンドボックスをプロジェクト直下(`server/__mut__/`)に置くと、並行稼働中の別エージェントが「見覚えのないファイル」として検出して報告コストが発生する。**サンドボックスも隔離スクラッチに置く**よう pj-isolated-verify に明記したい(6.5R は自ら後始末済み・実コードは無改変)

- [x] 2026-07-23 09:40 【失敗→パターン】**中身が空の要素にスタイルを当てても、親が `align-items: baseline` だと高さ 0 に収縮して何も描画されない**(`::before` の `top:0;bottom:0` も親のボックス基準なので面積 0)。`min-height` は baseline 整列では効かず、交差軸整列を `stretch` にする必要がある。この型は**コードレビューでもユニットテストでも原理的に捕まらず、実機で `getBoundingClientRect()` を測ってはじめて分かる**(6.5V' が実測で検出)
- [x] 2026-07-23 09:40 【成功パターン】UI の見た目に関する指摘を修正したら、**同じ verifier に「自分の所見が解消したか」を判定させる**と、CSS が入っただけで効いていない事態を捕まえられる。「修正した」と「直った」は別物(6.5V' が最優先項目として不合格判定)

- [x] 2026-07-23 11:20 【成功パターン】検証の健全性は**実証だけでなく構造的な導出**とセットで示すと強い。6.5R は「区切りバイトを同じ位置に埋め戻すから内部整列は常に原本どおり → デコード後の改行数は本物の改行数と厳密に一致 → 食い違うのは余分な `0x0A` がある場合に限られる」と導出したうえで 2,094 ケースの実証を添えた。導出があると「サンプルが足りないのでは」という疑いが残らない
- [x] 2026-07-23 11:20 【成功パターン】**修正を打ち消す変異**(R-1〜R-3 の修正そのものを元に戻す変異)をテストが殺せるかを見ると、「その修正が回帰テストで守られているか」を機械的に言える。6.5R は N1〜N5 で全て KILLED を確認 — 修正の永続性を数字で保証できる
- [x] 2026-07-23 11:20 【摩擦】async + 実 git 依存の関数は純関数テストの網に載らず、判定ロジックが無テストで残る(6.5 の `notFound` 判定)。この codebase には **2.4 で `checkApplyHunksRequest` を純関数に切り出した先例**があるので、ルート/IO 関数の中の判定は最初から純関数に切り出す規約にしたい — pj-git-route 追記候補
- [x] 2026-07-23 11:20 【失敗→パターン】エラー種別を `String(e).includes('...')` で判定すると、**引用されたパス名の中の文字列にも当たる**(6.5R が「no such path.txt」という名のファイルで実証)。先頭アンカー付き(`/fatal: no such path '/`)で照合する
- [x] 2026-07-27 【失敗→パターン】ファイルツリーの新規作成用インライン入力が 1〜2 秒ごとに消える不具合。原因は `FileTree.tsx` の `Node` を**コンポーネント関数の内側で定義**していたこと。react-arborist は `props.children` を「要素タイプ」として描画するため、親の再レンダーごとに identity が変わり subtree が unmount→remount される。ポーリング(App 4s / useTerminalSessions 3s)は引き金にすぎない。**行レンダラーをコンポーネント内で定義しない**を規約化したい(pj-* 候補)
- [x] 2026-07-27 【摩擦】uncontrolled な入力(`value` なし + `autoFocus`)は remount されても「フォーカスは残り値だけ消える」ため、原因が入力処理側にあるように見えて誤誘導される。入力欄の不具合は「値が消えたか」より **DOM ノードが差し替わったか**(要素参照の同一性)を先に測ると一発で切り分けられる
- [x] 2026-07-27 【成功パターン】ポーリング起因が疑われる UI 不具合は、隔離環境で「①値のクリア時刻列 ②要素参照の同一性反転 ③API リクエスト送出時刻」の 3 系列を同時採取すると、因果が 3 方向から裏付けられて仮説の反証余地が消える
- [x] 2026-07-27 【失敗→パターン】CDP 検証で「blur / フォーカス遷移」を検証するとき、`element.dispatchEvent(new MouseEvent(...))`(untrusted)ではブラウザーのデフォルトのフォーカス移動が起きず、**正常な実装を誤って NG と判定しかけた**。フォーカス・blur・クリック起因の検証は必ず `Input.dispatchMouseEvent`(trusted event)を使う — pj-isolated-verify 追記候補
- [x] 2026-07-27 【驚き】自作 CDP クライアントで `Runtime.evaluate` に `replMode: true` を付けると `awaitPromise` が効かなくなり即座に空値が返る。非同期評価を使うヘルパーでは `replMode` を付けない — pj-isolated-verify 追記候補
- [x] 2026-07-27 【成功パターン】「ポーリングを止めただけの見かけの修正」を排除するため、修正後の検証項目に **API リクエストが従来周期のまま送出され続けていること**を明示的に入れた。症状消失の確認だけでは、原因を潰したのか引き金を潰したのか区別できない
- [x] 2026-07-27 【驚き】.editorconfig は**マッチする全セクションをマージ**するため、拡張子が明記されていないファイルにも `[*]` の `end_of_line` が継承される。「対象拡張子の記載が無い ⇒ editorconfig は無関係」というのは誤った直感で、調査の初手で editorconfig を除外すると原因を見落とす。実測: `[*] end_of_line=lf` のみの .editorconfig で `sample.foo` → `{"end_of_line":"lf",...}`
- [x] 2026-07-27 【失敗→パターン】保存時整形(formatOnSave)が**ユーザーの明示操作を無条件で上書き**する設計は不具合になる(FilesTab.tsx:107-116 が ステータスバーの pushEOL(CRLF) を保存直前に LF へ戻す)。設定ファイル由来の値とユーザーの明示操作が衝突する箇所では、**明示操作 > 設定ファイル** の優先順位を明示的に実装する(上書きフラグを持たせる)
- [x] 2026-07-27 【摩擦】`file.editorconfig` は**ファイルオープン時のスナップショット**で、.editorconfig を編集しても開いているタブには反映されない(タブを閉じて開き直すまで)。設定ファイルの内容をレスポンスに埋め込んでキャッシュする設計では、その設定ファイル自身の保存が無効化トリガーになっていないと「直したのに効かない」体験になる
- [x] 2026-07-27 【失敗→パターン】scout(Haiku)の「最も疑わしい箇所」ランキングは**当てにしない**。今回サーバー側の 2 件を確信度「高」と報告したが実際は無罪(サーバーは content をそのまま書くだけ)で、真因は別の質問(Q5)の回答本文に事実として現れていた。**列挙された事実は使い、ランキングは Conductor が自分で組み直す**
- [x] 2026-07-27 【成功パターン】ビルドを挟む検証では、**開始前に成果物へ新規識別子が含まれるか grep する**(今回は `client/dist` に `/api/fs/editorconfig` の文字列があるか)。「古いバンドルを検証して合格にする」事故を機械的に排除できる。verifier が自主的に実施 — pj-isolated-verify 追記候補
- [x] 2026-07-27 【驚き】保存時整形に「オープン時スナップショットの設定」を使う設計では、**設定ファイル自身の保存が自己参照で 1 世代遅れる**。`end_of_line = crlf` と書き換えたその保存自体は、まだ `lf` だった旧スナップショットで整形されるため LF で書かれる。設定ファイルを自分自身の設定で整形する経路は「更新は次回から」になることを前提に設計・説明する

- [x] 2026-07-27 【失敗→パターン】scout の所見が誤ったのは「ランキングだから」ではなく、**この codebase に "タブ" と呼ばれる別物が 2 つある**ため(タイルのペイン切替 `TileView = files|git|term` と、FilesTab 内部のファイルタブ `tabs[]`)。scout は事実(TileView の全 kind)は正しく列挙したが、推奨先を誤った概念に接続した。**同名概念が複数ある領域では、Conductor が brief の時点で「どちらを指すか」を名指しで固定する**(今回 architect には「TileView の新 kind ではない」と明記して回避)
- [x] 2026-07-27 【成功パターン】`npm audit` の警告を「自分が入れた依存のせいか」切り分けるには `npm audit --json` の **`nodes` フィールドで実際のパスを見る**。今回 dompurify の advisory は range `<=3.4.11` で、該当ノードは `node_modules/monaco-editor/node_modules/dompurify@3.2.7`(monaco の入れ子・既存)のみ。トップレベルに入れた 3.4.12 は無罪だった。件数だけ見て「依存を足したら脆弱性が増えた」と誤報告する事故を防げる
- [x] 2026-07-27 【成功パターン】セキュリティ境界にライブラリを選ぶとき、**advisory の一覧はそのライブラリの「使ってはいけない機能」のリスト**として読める。DOMPurify の既知 advisory はほぼ全部が `IN_PLACE` / `ADD_TAGS` の関数形式 / `USE_PROFILES` / hook による設定汚染に集中していたので、builder への制約として「これらの機能を使わない」を明示できた。バージョンを上げるだけで満足しない
- [x] 2026-07-27 【驚き】`monaco.editor.colorize()` の `.mtkN` 配色 CSS は**エディターインスタンスが 1 度も生成されないと DOM に注入されない**(standaloneThemeService が生成時に `<style class="monaco-colors">` を作る)。「monaco が bundle されていれば colorize は単体で使える」は誤った直感で、プレビュー専用画面ではモノクロになる。検証項目に「エディタータブを一度も開かずに」を入れないとすり抜ける
- [x] 2026-07-27 【失敗→パターン】Windows の Git Bash から `curl --get --data-urlencode "path=画像/図1.png"` を投げると**非 ASCII が壊れてサーバーが 404 を返す**。サーバーは無罪で、原因は shell の引数エンコーディング。危うく T0 に虚偽のバグ報告を出すところだった。**非 ASCII を含む URL 検証は、shell に触らせず事前にパーセントエンコードした URL 文字列を直接渡す**(`path=%E7%94%BB%E5%83%8F%2F...`)。切り分け手順は「node で `fs.readdirSync` して実際のバイト列を hex で見る」+「pre-encoded URL で再試行」の 2 手 — pj-isolated-verify 追記候補
- [x] 2026-07-27 【成功パターン】builder の報告の「積み残し・懸念」欄は**最も情報密度が高い**。T4 が自主的に挙げた「`validateLink` を無効化した(セキュリティ境界の非自明な変更)」を Conductor が実地検証したところ、主張は正しかったが**より深い欠陥**(`classifyHref('')` が空 href をディレクトリーへの相対リンクに誤分類。markdown-it を通さなくても `[x]()` で再現する下位関数のバグ)が見つかった。懸念欄の主張は「正しいか」だけでなく「**その原因はもっと下にないか**」まで辿る
- [x] 2026-07-27 【失敗→パターン】subagent は「防御層が競合したとき、片方を無効化して一本化する」方向に倒しがち(T4 が markdown-it のスキーム検査を切って `classifyHref` に一本化)。二重防御は**役割が違うから 2 枚ある**ので、競合の解消は「片方を消す」ではなく「**両方が同じ安全側の結論に到達するよう下位の欠陥を直す**」。brief で二重防御を要求するときは、この解消方針まで書いておく
- [x] 2026-07-27 【驚き】**不可視の C0 制御バイトがソースに混入する事象を、別々の builder が独立に 2 回踏んだ**(T3 は制御文字レンジの正規表現リテラルを書こうとして、T4 は空文字列のつもりの箇所に)。どちらも typecheck も vitest も通過し、node でのバイト走査でしか見つからない。Conductor 自身も inbox にこの件を書こうとして Bash ツールの制御文字ガードに弾かれた(＝ Bash には防壁があるが Edit/Write には無い)。**制御文字を扱うコードを書かせたら、完了報告の時点で Conductor がバイト走査を回す**(`c<9||(c>13&&c<32)||c===127` を数える)。実装は正規表現リテラルでなく文字コード比較のループで書かせるのが予防策 — pj-* 追記候補
- [x] 2026-07-27 【驚き】markdown-it の `validateLink` が false を返すと、実装(`rules_inline/link.mjs`)は href を空にするだけでなく**パース位置を進めないためリンク構文全体がテキストにフォールバックする**。結果 `[x](javascript:alert(1))` は `<a>` 要素すら生成されない。「危険な href が空文字で残る」という自然な想定は誤りで、実際に危険なのは元から空/ディレクトリーのみの `[x]()` `[x](.)` だった。ライブラリの防御挙動は推測せず実際の出力を並べて確認する
- [x] 2026-07-27 【摩擦】スクラッチ配下に置いた使い捨て vitest 設定からプロジェクトのモジュールを import して検証すると、**third-party 依存の解決経路が変わって挙動が化ける**ことがある(T5 が遭遇: プロジェクト外テストを起点にすると `render.ts` 内の markdown-it の挙動が本番と一致しなかった)。切り分けは「同じスクラッチ設定で本物のテストファイルを走らせて green か」を見る。**スクラッチ検証で得た third-party がらみの否定的所見は、本番の `npm test` で再現するまで所見として採用しない** — pj-isolated-verify 追記候補
- [x] 2026-07-27 【失敗→パターン】**「正規化・デコードの順序」はセキュリティ判定の定番の穴**。`classifyHref` は生の文字列でスキーム判定し、パーセントデコードは後段の `resolveRelative` 内で走っていたため、`java%09script:` `%6Aavascript:` が全て `blocked` をすり抜けた(`%6A` はデコードすると `javascript:` そのもの)。生の制御文字(`java\tscript:`)は潰せていたのに、そのエンコード形が抜けていた。**「危険な入力の正規化形」ではなく「正規化してから判定」の順序にする**。判定と正規化が別関数に分かれている設計では特に踏みやすい
- [x] 2026-07-27 【成功パターン】敵対的フィクスチャを Conductor が**実装前に**作っておくと、builder の自己検証で拾えなかった欠陥が統合段階で確実に出る(今回 B3 の難読化スキームが T8 の実機検証で発火)。さらに builder の報告(1 パターン)を Conductor が自分で横展開したら**5 パターンに広がった**。**報告された欠陥は必ず「同じ原因の別の入り口」を自分で探す**
- [x] 2026-07-27 【成功パターン】reviewer に**推奨修正そのものの副作用まで書かせる**と価値が跳ね上がる。今回 R-1(エラーが成功時にクリアされない)に対し「素直に `error: ''` を足すと、`source` が前回と同一文字列なので effect が再実行されず本文が空になる第 2 のバグが出る」まで指摘してきた。**修正案は「直るか」だけでなく「直したとき何が壊れるか」を必ず添えさせる**
- [x] 2026-07-27 【摩擦】このプロジェクトは `vitest.config.ts` の include が `client/src/**/*.test.ts` のみで **`.tsx` が対象外**、かつ `@testing-library/react` も無いため、**React コンポーネントの挙動は構造的にユニットテストで守れない**。今回の reviewer 指摘 R-1〜R-3 は全てコード読解ベースで、回帰テストを書く手段が無い。コンポーネントに寄せた不具合は「実機検証でしか捕まらない・回帰も防げない」ことを前提に、検証項目を厚くするか、テスト基盤の追加を別件で検討する
- [x] 2026-07-27 【失敗→パターン】builder が「実測での比較はしていませんが、アーキテクチャ上そうなる設計です」と書いた断定は**実際に誤りだった**(json フェンスが 1 色なのを「monaco の JSON トークナイザーの特性」と断定 → verifier が『更新ボタンを押すと 28 span で正しく着色される』と反証。真因はコールドスタート時の遅延登録)。**「未実測だが原理上こうなるはず」は所見ではなく仮説**として扱い、仕様として記録しない。反証は「同じ操作をもう一度やる」だけで済むことが多い
- [x] 2026-07-27 【成功パターン】検証フィクスチャの中に**合格基準そのものを書き込んでおく**と、verifier が独自判断で基準を緩めるのを防げる。今回 `gfm.md` に「エディタータブを一度も開かずにプレビューだけ開いた状態でも着色されること(モノクロなら不合格)」と書いておいたため、ブリーフ本文が名指ししていなかった json の失敗も verifier が自力で不合格判定できた
- [x] 2026-07-27 【失敗→パターン】**同一関数(`classifyHref`)で「正規化と判定の順序」欠陥が 3 回連続で出た**: (1) パーセントデコードがスキーム判定より後 (2) `\` をパス区切りとして正規化していない (3) バックスラッシュ正規化がプロトコル相対 `//` 判定より後。**1 件直すたびに同じ型が別の入り口で再発する**ので、この型を見つけたら「その 1 件を直す」ではなく**「正規化を全部先に済ませてから全判定を走らせる」構造に作り直させる**のが正解。個別修正を繰り返すと修正サイクルを浪費する(今回 3 サイクル消費)
- [x] 2026-07-27 【成功パターン】builder の修正報告を Conductor が**独立に自作プローブで再検証**したところ、報告では全件通過とされた修正に 1 件の穴が残っていた(エンコード UNC `%5C%5Chost%5C...`)。**セキュリティ境界の修正は、builder のテストが green でも Conductor が自分の入力セットで叩き直す**。builder のテストは builder が想定した攻撃面しかカバーしない
- [x] 2026-07-28 【驚き】react-arborist の**開閉状態は `data` prop から独立した内部ストア**(`state.nodes.open.unfiltered`、ノード id がキー)に保持され、`data` を丸ごと差し替えても消えない。そのためツリーの再読み込みで `children` を `null` に戻すと「シェブロンもアイコンも開いた見た目のまま中身が 0 件」になり、ユーザーは閉じる→開くの二度手間を強いられる。一般化: **サードパーティの表示コンポーネントが持つ内部 UI 状態(開閉・選択・スクロール)は data の作り直しでは消えない**ので、再取得系を実装するときは「データと UI 内部状態の両方を整合させる」ことを設計項目に立てる — pj-client-ui-state 追記候補
- [x] 2026-07-28 【摩擦】CDP でのクリック検証で `Input.dispatchMouseEvent` の **`mouseMoved` を挟むと 1 発ごとに約 5 秒の遅延**が乗ることがある(実測: 4 連続とも 5011〜5014ms。Chrome/接続固有で再現性あり)。連打・二重発火・競合系の検証で「連打したつもりが 17 秒間隔」になり、**製品側の stale 対策が効いているのか単に間隔が空いていただけなのか区別できなくなる**。`mousePressed`/`mouseReleased` のみでも通常クリックは正常に発火するので、タイミング依存の検証では `mouseMoved` を外す(フォーカス/hover 依存の検証では必要なので使い分ける) — pj-isolated-verify 追記候補
- [x] 2026-07-29 【摩擦】pj-isolated-verify の「スクラッチは短パスに置く」(MAX_PATH 対策、罠 5) に従って `C:\vt-ft` にフィクスチャを作ると、**後片付けの `Remove-Item` がハーネスのトップレベルパス保護に弾かれて削除できない**(`Remove-Item on system path 'C:\vt-ft' is blocked`)。2 つの規約が衝突している。→ **2026-07-29 ユーザー判断で解決**: 作業領域をリポジトリー直下の `vt/`(verify test、`.gitignore` 済み)に集約する規約に変更し、pj-isolated-verify の手順 1・罠 5/6/7・後始末に反映。ドライブ直下は今後使わない
- [x] 2026-07-29 【驚き】inbox に記録済みの制御バイト走査式 `c<9||(c>13&&c<32)||c===127` は **0x0b(垂直タブ)と 0x0c(改ページ)を素通しする**(9-13 を丸ごと許容しているため)。実際、この式で「制御バイトなし」と判定した直後にフックが 0x0b を検出した。混入元は `C:\vt5` の `\v` がどこかの層でエスケープ解釈された残骸で、**コミット済みのまま複数の grow サイクルを生き延びていた**。正しい条件は「0x20 未満のうち 0x09(タブ)と 0x0a(LF)と 0x0d(CR) 以外、および 0x7f」。**記録済みの検出式そのものが穴を持つ**という型なので、走査式は「許可リスト方式(通してよいバイトを列挙)」で書く
- [x] 2026-07-29 【失敗→パターン】PowerShell でファイルをバイト単位修正する際、`$list.AddRange($bytes[0..$n])` は配列スライスが `object[]` になるため **MethodException で失敗するが、スクリプトは停止せず後続の `WriteAllBytes` が実行される**。結果 37KB のファイルが 2 バイトに切り詰められた(git 管理下だったため `git checkout HEAD -- <file>` で復旧、未コミットの 1 行のみ喪失)。**バイト単位の書き換えは (1) 一時ファイルに書く (2) サイズと再走査で検証する (3) 検証が通ってから原本に上書き、の 3 段階で行う**。原本を直接 open して書くルートを取らない
- [x] 2026-07-29 【失敗→パターン】**設計のストレステスト(Plan エージェント)が走っている最中に ExitPlanMode を呼んで承認を取ってしまった**。直後に届いたレビューが承認版の設計に P0 を 3 件検出(worktree 跨ぎでの作業ブランチ黙示破壊 / remote 引数経由の任意コマンド実行 / 三角ワークフローへの誤送信)。承認は「レビュー未了の成果物」に対して取ってはいけない。**自分が発注した検証がまだ返っていないなら、承認ゲートは開けない**(待てないなら、その旨を明示して承認を求める)
- [x] 2026-07-29 【驚き】`git fetch "--upload-pack=echo pwned" <remote> <refspec>` は**実際にコマンドを実行する**(実測: `fatal: protocol error: bad line length character: pwne` = echo の出力が git protocol として読まれている)。`--` を remote の手前に置くと `fatal: strange pathname ... blocked` で止まる。`fetch`/`push`/`pull --ff-only` の 3 つとも remote 手前の `--` を受理することを実測済み。さらに **`git check-ref-format refs/heads/-x` は exit 0**(先頭 `-` の refname は正当)なので、既存コメントにある「git 由来の値だから安全」という論法は成立しない。git 由来でも先頭 `-` ガードと `--` を両方かける — pj-git-route 追記候補
- [x] 2026-07-29 【驚き】`%(upstream:track)` の `ahead N`/`behind M`/`gone` を翻訳するのは `setup_ref_filter_porcelain_msg()` で、これは **`builtin/branch.c` からしか呼ばれない**。つまり `git branch --format` は翻訳され、**`git for-each-ref --format` は構造的に非翻訳**。ロケール依存を env(`LC_ALL=C`)で潰すより**コマンドを plumbing 側に変える**ほうが正しく、副作用(detached HEAD 擬似行が消える・他の UI 文言を英語化しない)まで一緒に解決する。**「env で挙動を固定する」案が出たら、まず同等の plumbing コマンドが無いか探す**
- [x] 2026-07-29 【驚き】`git branch -a --format=...` は detached HEAD に対して `(HEAD detached at 1a2b3c)` という **ref ではない行**を出し、これが `BranchInfo` に `current: true` で混入している(既存バグ。`AddWorktreeModal.tsx:22` の `list.find(b => b.current)` が拾って worktree 作成が失敗する)。`%(refname:short)` は `refs/remotes/origin/HEAD` を **`origin`** に短縮するため、既存の `endsWith('/HEAD')` 除外もすり抜ける。**ref 一覧のパースは「名前の形」でなく `refs/heads/` `refs/remotes/` の前置と `%(symref)` の有無で構造的に判定する**
- [x] 2026-07-29 【失敗→パターン】**レビュアー(Plan エージェント)の推奨修正そのものが誤っていた**。「`%(push)` が空 ⇔ 素の git push が壊れているケース」という検出器を提案され、根拠として「三角ワークフローでは `%(push)` は非空」と断定されていたが、実フィクスチャで測ったら**三角(`branch.X.pushRemote`)でもローカル追跡(`remote=.`)でも `%(push)` は空**だった。そのまま実装していたら、正常動作中の三角ワークフローを `origin` へ誤送信し、ローカル追跡では自リポジトリーの main を書き換えていた。正しい判別軸は `%(push:remotename) === %(upstream:remotename)`(= 非三角)+ `remotename !== '.'`。**レビュアーが「この検出器で判別できる」と言ったら、その検出器を全分岐の実データに当てて真理値表を自分で作る**。推奨の結論だけでなく、推奨が依拠する事実主張を測る — pj-git-route 追記候補
- [x] 2026-07-29 【成功パターン】敵対的フィクスチャを実装前に作り、**P0 を実コマンドで再現してみせた**のが決定的だった(w2 の `wt-feature` が `git pull --ff-only origin refs/heads/main` で origin/main へ黙って早送りされ、git は "Fast-forward" と成功報告する)。設計レビューの指摘は文章では「起こり得る」に留まるが、**再現コマンドと before/after のハッシュまで出すと、修正の要否が議論の余地なく確定する**。フィクスチャは合格基準を README に埋め込む運用(pj-isolated-verify)と組み合わせると、検証者が基準を緩められなくなる
- [x] 2026-07-29 【摩擦→要判断】pj-git-route は「`server/index.ts` は import しただけで実サーバーと PTY が起動するので supertest は入れず、検証ロジックを純関数に切り出せ」としているが、切り出し先を**同じ index.ts のまま export する**と結局 import が必要になる。builder は `vi.mock('node:http')` で `createServer` を no-op にして回避した(実測: 実 config のタイムスタンプ不変・2.6 秒で正常終了・ポート bind なし。`PtyManager` は `setInterval().unref()` のみ、`config.ts` は module スコープで I/O 無し、が効いている)。**今は安全だが、後日 index.ts の module スコープに I/O が足されると気付かず踏む**。恒久策は検証純関数を `server/validate.ts` 等の別モジュールへ出してモックを不要にすること。pj-git-route §2 に「切り出し先は index.ts ではなく別モジュール」と 1 行足す候補
- [ ] 2026-07-29 【摩擦】Bash ツールから `node -e '<複数行スクリプト>'` を実行すると**何も出力せず正常終了する**(exit 0、stdout 空)。2 回続けて踏み、検証結果が「合格」なのか「実行されていない」のか区別できなかった。回避は**スクリプトをファイルに書いて `node <file>` で実行する**(scratchpad に .cjs で置くと確実)。一般化: **検証スクリプトが無出力で終わったら「合格」と読まない**。必ず「スキャンした対象と件数」を先に出力させ、無出力＝実行失敗と判別できる形にする — pj-isolated-verify 追記候補
- [ ] 2026-07-29 【失敗→パターン】inbox の一括チェックオフを Python の `io.open(p,'w')` で行ったら、**newline 変換で LF ファイル全体が CRLF 化**し 12 行の変更が 113 行の差分になった。さらに `'- [ ] '` の単純置換がヘッダーのフォーマット説明文(`Format: \`- [ ] date | mission ...\``)まで書き換えた。**行頭アンカーなしの一括置換とテキストモード書き込みはセット**で事故になる。復旧は自分のメモリー規約どおり「バイトで読む → 置換 → 一時ファイル → 検証(CR 数・行頭一致数・ヘッダー無傷) → 上書き」で実施。**Markdown の一括編集も「バイト単位書き換え」と同じ危険度で扱う**
- [ ] 2026-08-01 【驚き】task: memleak-survey | **WebGL コンテキストのリークは JS ヒープに写らない**(GPU/ブラウザープロセス側)ため heap snapshot では無実に見える。「長時間で遅くなる・ブラウザー再起動で直る」系の調査は、ヒープより先にコンソールの `webglcontextlost` 検索と `.xterm-screen canvas` 数の突合を見る。実測: 17 端末目で Chrome が最古 2 端末を無音降格、**表示中のアクティブタブ自身も降格対象になった**
- [ ] 2026-08-01 【摩擦】task: memleak-survey | Volta 環境で USERPROFILE を差し替えると、既知の tsx.cmd だけでなく **`node_modules/.bin` 経由の node 直叩きもシムで同死**する。`$LOCALAPPDATA/Volta/tools/image/node/<ver>/node.exe` の実体パス直指定まで掘る必要があった(verifier 実測)— pj-isolated-verify 罠 2 の追記候補
- [ ] 2026-08-01 【成功パターン】task: memleak-survey | 期待結果の事前登録に**対照ケース(≤16 端末では発火しない)を含めた**ことで、判定式自体の誤り(canvas 数=端末数と想定 → 実際は xterm 5.5 が link-layer 用 2D canvas を常設し ×2)が対照測定の段階で発覚し、実データから数式を再構成してから本判定に進めた。事前登録+対照は仮説だけでなく**判定式のバグ**も捕まえる
- [ ] 2026-08-01 【驚き→成功】task: memleak-fix | Edit の old_string 不一致で Write に切り替えた builder が、元コードの **U+00A0(NBSP)を通常スペースに取り違えて上書き**しかけた(`{line || ' '}` の ' ' が実は NBSP)。builder 自身が `git show HEAD:` とのバイト比較で検出し復元。**Edit が不一致になったとき、その原因はしばしば不可視文字 — Write で書き直す前に不一致の原因バイトを特定する**を規約化したい。既存の「バイト検証ゲート」が Write 経由の書き直しにも適用されるべき事例
- [ ] 2026-08-01 【失敗→パターン】task: memleak-fix | **React 18 は unmount 時、ref のデタッチ(null 化)が passive effect cleanup より先**。cleanup 内で `containerRef.current` に依存する後始末(canvas 捕獲→loseContext)は unmount 経路だけ静かに不発になる。しかも **StrictMode の疑似 unmount では ref が生きているため dev では動いて見える**、最悪の形。effect 冒頭のローカル変数を cleanup に渡すのが正解(reviewer が React 18.3.1 実測で検出)— pj-client-ui-state 追記候補
- [ ] 2026-08-01 【失敗→パターン】task: memleak-fix | レビュー指摘の修正(名前空間 `${leafId}/conflict/${path}`)自体が新規 P1 を混入 — **名前空間の判別子をユーザーのパス空間内のセグメントに置くと、実在パス `conflict/...` と URI が完全一致**し、Monaco モデル取り合い→別ファイル誤保存まであり得た。判別子は**ユーザー入力が届かない第 1 セグメント側**(`${leafId}-conflict/`)へ。修正サイクル 2 回目の再レビューが実物 URI.parse の真理値表で捕捉 — 「修正の再レビュー」の価値の実例
- [ ] 2026-08-01 【摩擦】task: memleak-fix | localStorage フィクスチャのシードは `setItem → Page.reload()` では**旧 React インスタンスの unmount flush が seed を上書き**して消す(実測: 復元 0 件)。`Page.addScriptToEvaluateOnNewDocument`(次ナビゲーションで React 起動前に実行)でシードするのが正解 — pj-isolated-verify 追記候補
- [ ] 2026-08-01 【摩擦】task: memleak-fix | headless Chrome の既定ビューポート(764×429)は複数ペイン UI に狭すぎ、**要素が数十 px に潰れて trusted クリックが別要素に当たり「ボタン無反応」に見える**。製品バグと即断せず、`el.click()` 対照・スクリーンショット・getBoundingClientRect で切り分け、`Emulation.setDeviceMetricsOverride` で 1400×900 に広げて再実行 — pj-isolated-verify 追記候補
- [ ] 2026-08-01 【驚き】task: tab-middle-click | `TileGrid` は `TileWorkspace` を `createPortal` で **`TilePane` の React 兄弟**として描画するため、React 合成イベントは fiber の return パスを辿り `TilePane.tsx:74` の `onMouseDownCapture`(`focusLeaf`)を**通らない**。DOM 上は祖先でも合成イベントの祖先ではない。結果、**タイル内コンテンツ(ファイルタブ・ターミナル)のクリックでは `focusedLeafId` が更新されていない既存の潜在バグ**がある(`.focused` リングと `adoptSessions` の孤児採用に影響。今回は範囲外として未修正)。一般化: **ポータルを使う設計では「DOM の祖先 ≠ React イベントの祖先」**。祖先要素にハンドラーを置く設計判断は、間にポータル境界がないか確かめてから採る — pj-client-ui-state 追記候補
- [ ] 2026-08-01 【摩擦】task: tab-middle-click | ブリーフで `const { confirm, dialog } = useConfirm();` と**ローカル変数名まで指定した**ら、同じファイルに「スコープ外・変更禁止」と指定したネイティブ `confirm()` 呼び出しがあり、**識別子シャドウで型エラー**(ブリーフ内の 2 指示が両立不能)。builder が `GitTab` の既存回避パターン(`confirm: confirmDialog`)に自力で合わせて解決し、逸脱として報告してきた(正しい対応)。**ブリーフで変数名を指定するときは、そのファイル内の既存識別子——特にグローバルの `confirm`/`name`/`status`/`length` 等——との衝突を先に grep する**。名前を指定しないか、指定するなら衝突確認までがセット
- [ ] 2026-08-01 【驚き】task: tab-middle-click | `.tile-pane` の `container: tile / inline-size`(styles.css:1418)は **`position:fixed` の包含ブロックを作る**(CSS containment 仕様)。そのためタイル内から描画される `.modal-backdrop`(`position:fixed; inset:0`)はビューポートではなく**タイルに閉じ込められ**、`.tile-host { overflow:hidden }` でクリップされる。`width:460px` 固定の `.modal` は狭いタイルでボタンが画面外に出て操作不能になる。**既存の ConfirmDialog 利用箇所(GitTab/HistoryTab/ChangesTab/ConflictResolvePane/DiffHunkStrip)すべてが同じ欠陥を抱えていた**が、Git タイルが広いことが多く発覚していなかった。`createPortal(…, document.body)` で解消し、幅 187px のタイルで backdrop 1584×905(ビューポート一致)・ボタン実クリック可を実測。**タイル内コンポーネントからモーダル/ポップオーバーを出すなら body へのポータルが必須**(`PromptDialog` と他モーダルは未対応のまま残っている) — pj-client-ui-state 追記候補
- [ ] 2026-08-01 【摩擦】task: tab-middle-click | 検証ブリーフに**対照条件を実測せずに書いた**: 「`.term-body` のミドルクリックは `defaultPrevented === false` のままであること」としたが、実際は xterm.js 自身がミドルクリックペースト用に `preventDefault()` しており **`true`** だった。verifier が「今回の diff に該当コードなし」まで切り分けて正しく処理したので実害はなかったが、**未実測の対照条件は verifier を誤判定させる**。ブリーフに対照を書くなら「〜であること(合格基準)」ではなく「〜を観測して報告せよ」と書くか、Conductor が先に実測して基準を確定する — pj-isolated-verify / brief 追記候補
- [ ] 2026-08-01 【驚き】task: tab-middle-click | 検証中に**今回の変更と無関係な既存バグ**を検出: diff タブを左クリックで切り替えるだけで `Uncaught Error: TextModel got disposed before DiffEditorWidget model got reset` が発生する(6 回記録 → ミドルクリック機能を一切使わない左クリックのみの往復 5 回で 3 回再現し、切り分け完了)。Monaco `DiffEditorWidget` の破棄処理の競合状態。**未修正・未報告のまま**。検証のコンソール監視をベースライン付きで回すと、対象機能の外側にある既存欠陥まで拾える(拾ったら「無関係」で捨てずに記録する)
